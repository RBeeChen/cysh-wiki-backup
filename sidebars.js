/** @type {import('@docusaurus/plugin-content-docs').SidebarsConfig} */
const sidebars = {
  // 1. 所有條目：顯示全部
  tutorialSidebar: [{type: 'autogenerated', dirName: '.'}],

  // 2. 嘉中老師：你可以將老師的檔案放在 docs/teachers 資料夾，
  // 或者在這裡手動羅列（如果檔案很多，建議移動資料夾）
  teacherSidebar: [
    {
      type: 'category',
      label: '嘉中老師',
      collapsed: false,
      items: [
        // 這裡會自動抓取 docs 內所有檔名包含「老師」的檔案
        // 註：Docusaurus 內建過濾較弱，若要完全自動，建議將檔案移至單一資料夾
        // 目前我們先採手動或目錄結構方式
        {
          type: 'autogenerated',
          dirName: '.', // 如果你把老師檔案移到 docs/teachers，這裡改為 'teachers'
        },
      ],
    },
  ],

  // 3. 嘉中社團
  clubSidebar: [
    {
      type: 'category',
      label: '嘉中社團',
      collapsed: false,
      items: [
        {
          type: 'autogenerated',
          dirName: '.', // 同理，建議未來移至 docs/clubs
        },
      ],
    },
  ],
};

export default sidebars;